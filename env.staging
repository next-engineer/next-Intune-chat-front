# ========================================
# 스테이징 환경 변수 설정 파일 (AWS)
# 테스트 및 검증 환경용
# ========================================

# API 설정
# AWS API Gateway 스테이징 엔드포인트
VITE_API_URL=https://your-api-gateway-url.execute-api.ap-northeast-2.amazonaws.com/staging

# WebSocket 설정
# AWS WebSocket API Gateway 스테이징 엔드포인트
VITE_WEBSOCKET_URL=wss://your-websocket-api-gateway-url.execute-api.ap-northeast-2.amazonaws.com/staging

# Notion API 설정
# Notion API 키 (실제 값으로 변경 필요)
VITE_NOTION_API_KEY=your-notion-api-key
# Notion 데이터베이스 ID (실제 값으로 변경 필요)
VITE_NOTION_DATABASE_ID=your-notion-database-id

# 스테이징 환경 설정
VITE_DEV_MODE=false
VITE_DEBUG_MODE=true

# 기능 플래그 (스테이징에서 테스트할 기능들)
VITE_ENABLE_CHAT=true
VITE_ENABLE_NOTION=true
VITE_ENABLE_PINGPONG=true

# 타임아웃 설정 (스테이징에서는 중간 타임아웃 사용)
VITE_API_TIMEOUT=12000
VITE_WEBSOCKET_PING_INTERVAL=30000
VITE_WEBSOCKET_PONG_TIMEOUT=10000

# 빌드 설정
VITE_APP_NAME=Intune Front (Staging)
VITE_APP_VERSION=0.1.0

# AWS 전용 설정
# AWS 리전 (한국: ap-northeast-2)
VITE_AWS_REGION=ap-northeast-2
# AWS Cognito 사용자 풀 ID (실제 값으로 변경 필요)
VITE_AWS_COGNITO_USER_POOL_ID=your-cognito-user-pool-id
# AWS Cognito 클라이언트 ID (실제 값으로 변경 필요)
VITE_AWS_COGNITO_CLIENT_ID=your-cognito-client-id
# AWS S3 버킷 이름 (스테이징용, 실제 값으로 변경 필요)
VITE_AWS_S3_BUCKET_NAME=your-s3-staging-bucket-name
# AWS CloudFront 배포 ID (스테이징용, 실제 값으로 변경 필요)
VITE_AWS_CLOUDFRONT_DISTRIBUTION_ID=your-cloudfront-staging-distribution-id
